# Unpublished archive

These files are retained for the owner in GitHub, but excluded from GitHub Pages by `_config.yml` and Jekyll's underscore-directory convention. Do not add `.nojekyll` or publish the repository root with a raw static host: doing so would expose the archive. Any future publishing system must explicitly omit this directory.

The 2026-09-08 archive contains all three previously published investigations and their source materials, the previous landing page, and Bar Trivia, whose question bank reproduces article content. Original article URLs intentionally return 404. Other games and live tools remain published. Git history stays public at the owner's request.
